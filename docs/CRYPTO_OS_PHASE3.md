# Crypto Ecosystem OS — Phase 3 ecosystem intelligence

Additive. EXECUTE off. Growth never assigns VERIFIED. Social velocity stays MISSING.

## Delivered

| Item | Behavior |
| --- | --- |
| Discovery | DexScreener pair counterparts (not WETH/stables/CEX). Status `discovered` / `watchlist` / `high_risk`. `auto_verified` always false. |
| Verification | Unverified → data → contract → ecosystem from **live evidence**. Never `community_verified`. **VERIFIED ≠ SAFE.** |
| Narratives | Registry tags + weak name heuristics. `mention_velocity` / `unique_accounts` = MISSING. Accel/decel only if live liquidity change exists. |
| Genome clusters | Cosine(genome) + Jaccard(narratives) + chain. Token name is not a feature. |
| Opportunities | Skip missing social/liquidity/dev. Every item includes “not a buy/sell recommendation”. |

## API / UI

`GET /v1/discovery` ` /v1/narratives` ` /v1/genome-clusters` ` /v1/tokens/{id}/verification`  
Pages: `/discovery`, `/narratives`.

Flags: `jobs.discovery`, `jobs.verification`, `jobs.narratives`, `jobs.genome_clusters`.

## Not this wave

Licensed social firehose, RAG / research agents, billing, SSO, Solana, automatic VERIFIED promotion.
