# AI research

**Now (Crypto OS Phase 4):** intent → lexical RAG → specialists → citation check → `research-agent/v1`. `POST /v1/research`, markdown/JSON. No live LLM required. Social without licence = INSUFFICIENT_EVIDENCE. EXECUTE cannot be enabled by prompt text.

**Target (V2 Phase 7):** same pipeline, inputs from **Twin state** (not providers). Extra agents: wallet, whale, ecosystem, simulation, evidence. Orchestrator + Investigate Next + Copilot (`POST /v2/copilot`). Eval stays mandatory (hallucination, citations, counter-evidence).

See [scoring-risk-ai.md](scoring-risk-ai.md) and [CRYPTO_OS_PHASE4.md](CRYPTO_OS_PHASE4.md).
